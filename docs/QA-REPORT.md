# QA-отчёт — Тайм-трекер (Этап 1 + Этап 2)

Дата: 2026-07-20 · QA: qa-verifier (Агент 3) · Разрешение проверки: 390×844, deviceScaleFactor 2 (Playwright/Chromium)

**Важно:** на машине нет ключей Supabase (`.env` — плейсхолдеры), облачная БД недоступна; wrangler не авторизован. Все критерии, требующие живой БД или деплоя, отмечены «ЗАБЛОКИРОВАНО» и проверены статически по коду. UI проверялся живьём в демо-режиме без БД: `npm run dev`, адрес `http://127.0.0.1:5173/?demo=1`.

## Сводка

| Итог | Кол-во |
|---|---|
| OK | 27 |
| Дефект | 2 |
| Заблокировано (нет ключей / wrangler) | 6 |

Сборка зелёная: `tsc --noEmit` — 0 ошибок, `npm run build` — успех (2.7 с). `npm run lint` — скрипта нет (дефект Д-2).

---

## Этап 1 — техничка

| Пункт ТЗ | Статус | Комментарий | Скриншот |
|---|---|---|---|
| `lint` | **ДЕФЕКТ (Д-2)** | `npm run lint` даёт `npm error Missing script: "lint"`; ESLint не установлен (нет в devDependencies, нет конфига) | — |
| `tsc --noEmit` | OK | Вывод пуст, exit 0 | — |
| `npm run build` | OK | Успех за 2.66 с. Замечание: чанк `index-*.js` 556.69 kB (больше 500 kB, предупреждение Vite) | — |
| Авторизация настоящая (email+пароль, НЕ anonymous) | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | Статически OK: `src/api/auth.ts` — `signUp`/`signInWithPassword`; `src/screens/Login.tsx` — формы входа/регистрации; anonymous нигде не используется. Без ключей показывается заглушка «заполните .env по SETUP.md» — ожидаемо | 10-login.png |
| Таймер: старт, идёт («35 минут прошло»), стоп | OK | В демо: старт, тикает `00:01:05`, подпись «Идёт 2-я минута. Не отвлекаемся», стоп работает | 06-timer-running.png |
| Режим (а): таймер на категорию без задачи | OK | «Саморазвитие», затем Старт — идёт по категории | 06-timer-running.png |
| Режим (б): таймер на задачу внутри категории | OK | «Школа», пилюля «Матеша», Старт — идёт по задаче; празднование при стопе | 12-stop-celebration.png |
| Сессия появляется в БД и в статистике | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | В демо статистика обновляется сразу (сегодня 2 ч 10 мин, после минуты таймера 2 ч 11 мин). Код записи: `src/api/sessions.ts` `startSession`/`stopSession` (insert/update в `tt_sessions`) | 07-home-running.png |
| Мультиюзер-изоляция (второй пользователь не видит чужого) | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | Статически OK: `supabase/migrations/001_create_core_tables.sql` — RLS включён на `tt_categories`/`tt_tasks`/`tt_sessions`, политики select/insert/update/delete `*_own` по `auth.uid()` | — |
| Таймер переживает перезагрузку страницы | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | Статически OK: `Timer.tsx:61`, `Home.tsx:63`, `Planet.tsx:68` вызывают `getActiveSession()` при маунте; `sessions.ts` ищет запись `ended_at is null` — активная сессия восстановится из БД. В демо данные in-memory и живут до перезагрузки — так задумано | — |
| Одна активная сессия; новый старт закрывает старую | OK (код) | `startSession()` сначала вызывает `stopSession()`; в БД partial unique index `tt_sessions_one_active_per_user` (001_*.sql:115). В UI — кнопка «Переключиться на выбранное (текущая сессия закроется)» | — |
| Статистика день/неделя/месяц сходится с `tt_category_totals()` | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | В демо цифры согласованы: Домой «Сегодня 2 ч 10 мин» = Статистика/День «2 ч 10 мин» (Финансы 1 ч + Школа 40 мин + Саморазвитие 30 мин); планета «всего 47 ч 30 мин» = сумма зон (35+10+2+0.5). RPC `tt_task_totals`/`tt_today_totals`/`tt_stats` есть в 002_*.sql, `tt_category_totals` защищена в 003_*.sql | 01-home.png, 03-stats.png, 04-planet.png |
| Сводка по каждой категории, в т.ч. 0 | OK | «Проекты — 0 мин · 0%» показывается в списке дня | 03-stats.png |
| CRUD категорий и задач | OK | Создание/редактирование в bottom-sheet, «Новая категория», «Добавить задачу»; удаление — архивация с undo-тостом «Вернуть» (`Categories.tsx:126,176`) | 05-categories.png |
| Деплой Cloudflare Pages + ссылка | **ЗАБЛОКИРОВАНО** | wrangler не авторизован — `npm run deploy` не запускался. Нужен `npx wrangler login`, затем `npm run deploy` (см. SETUP.md). После деплоя — smoke-прогон и ссылка сюда и в README | — |

## Этап 2 — редизайн и планета

| Пункт ТЗ | Статус | Комментарий | Скриншот |
|---|---|---|---|
| Фон — живая сцена, без плоских однотонных фонов | OK | Зелёная сцена с планетой и боке на всех экранах; на Таймере — в расфокусе | 02-timer.png |
| Все панели — стекло с `backdrop-blur`, два тона | OK | `src/index.css`: `.glass-dark` и `.glass-light` — `backdrop-blur-xl`, полупрозрачность, светлая кромка, radius 24 | 01-home.png |
| Лаймовый акцент + глубокий зелёный | OK | Кнопки Старт и «Новая категория», активная вкладка, цифры таймера — lime-300 | 05-categories.png |
| Пилюльные сегмент-контролы, активный скользит | OK | День/Неделя/Месяц (лаймовая пилюля), «Вся категория»/задачи; скольжение через framer-motion `layoutId` | 03-stats.png |
| Метки на сцене — белые пилюли с хвостиком | OK | «Финансы · 35 ч» и другие, с хвостиком, всегда «головой вверх» | 04-planet.png |
| Шрифты Unbounded (цифры/заголовки) + Golos Text | OK | Подключены в `index.html` (Google Fonts); `tailwind.config.js`: `font-sans` = Golos Text, `font-display` = Unbounded | 06-timer-running.png |
| Таб-бар: 5 вкладок, SVG-иконки, лаймовая пилюля | OK | Домой · Таймер · Статистика · Планета · Категории; пилюля скользит (`layoutId="tab-pill"`) | 01-home.png |
| Планета крутится и двигается пальцем | OK | `Planet.tsx` — onPointerDown/Move/Up + инерция; drag мышью в Playwright: rotate 0 гр. в 60.75 гр. | 04-planet.png |
| У категорий разные темы зон | OK | В демо 4 темы: шахта/корпорация/космопорт/банк; в `themes.ts` все 5 (+нефтевышка), свободные предлагаются по очереди (`suggestTheme`) | 04-planet.png |
| **Тап по зоне ныряет в интерьер** | **ДЕФЕКТ (Д-1)** | Реальный тап/клик по зоне НЕ открывает интерьер: `setPointerCapture` контейнера вращения съедает click (подробности ниже). Обработчик `onClick` кнопки исправен — программный `el.click()` открывает | 09-interior-mine.png |
| Интерьер: герой работает, бабл тикает | OK | Шахта: герой с киркой, алмазы в породе, вагонетка, бабл `00:00:14` «Герой при деле — минуты капают» (открыто программно из-за Д-1; также открывается по `?zone=`) | 09-interior-mine.png |
| Интерьер «спит» без сессии | OK | Корпорация без таймера: приглушённый свет (bg-black/45), «Тихо. Запустите таймер этой категории…» | 11-interior-corporation.png |
| Выход из интерьера — кнопка-стрелка | OK | Кнопка «Вернуться на планету» работает, зоны снова видны (проверено Playwright) | — |
| Чувачок: работает в зоне при сессии, отдыхает в центре без неё | OK | С таймером — герой у активной зоны + бабл `00:00:19`; без — «Отдыхает» в центре | 08-planet-running.png, 04-planet.png |
| Пульсирующая кайма активной зоны; планета доворачивается | OK | Кайма-кольцо видна у активной зоны; автодоворот к активной зоне в коде (`Planet.tsx:126-144`, не мешает при drag) | 08-planet-running.png |
| Панель «N зон · всего N ч» | OK | «4 зоны · всего 47 ч 30 мин» | 04-planet.png |
| Пороги 1/3/7/15/30/50 только из `thresholds.ts` | OK | Хардкодов часов в компонентах нет (grep чист; в `Exterior` — только номера уровней `level >= N`). Демо-уровни сходятся: 35 ч — Империя, 10 ч — Бизнес, 2 ч — Лагерь, 0.5 ч — Пустырь | 05-categories.png |
| Таймер: кольцо 240px, оборот за час, «Идёт N-я минута» | OK | `Timer.tsx:30` `RING = 240`; подпись «Идёт 2-я минута. Не отвлекаемся» | 06-timer-running.png |
| Стоп — празднование, при пороге — что построено | OK | Карточка «+1 мин — Зона „Школа“ подросла»; при пересечении порога — «Новый уровень» + `builtWhat` из `themes.ts` (код `Timer.tsx:361-397`) | 12-stop-celebration.png |
| Домой: приветствие + дата, пилюля «Сейчас идёт», сегодня + топ-3, «Продолжить», карточка планеты | OK | «Добрый день», «Понедельник, 20 Июля», «Сейчас идёт: Саморазви… 00:01:07», «Сегодня в зачёте 2 ч 11 мин» + топ-3, «Продолжить: Финансы · Инвест…» + «Другое…», «Моя планета». Пустое состояние «Чистый лист…» есть в коде (`Home.tsx:199`) | 01-home.png, 07-home-running.png |
| Статистика: сегмент-контрол, крупный итог, скруглённые столбцы, полосы с процентами, разрез по задачам | OK | Всё присутствует, включая «По задачам» (Инвестиции · Финансы — 1 ч) | 03-stats.png |
| Категории: список с темой/уровнем/временем, bottom-sheet, архивация с undo | OK | «Шахта · Пустырь · 30 мин» и т.п.; архивация с тостом «Вернуть» | 05-categories.png |
| Без эмодзи (SVG-иконки) | OK | Grep по эмодзи-диапазонам в `src/` — только типографские стрелки (не эмодзи); иконки — SVG (`src/ui/Icons.tsx`) | — |
| Без `confirm`/`alert` | OK | Grep чист; удаления — через архивацию с undo-тостом | — |
| Тексты русские | OK | Все экраны на русском | — |
| Цифры на планете и в статистике сходятся с `tt_category_totals()`/`tt_task_totals()` | ЗАБЛОКИРОВАНО: нет ключей — проверить после вставки `.env` по SETUP.md | В демо сходятся (см. Этап 1). RPC-вызовы: `src/api/stats.ts` | — |

---

## Дефекты (вернуть агенту по зоне)

**Д-1 (критично, зона frontend-ui): тап по зоне планеты не открывает интерьер.**
Место: `src/screens/Planet.tsx:152` — в `onPointerDown` контейнера вращения вызывается `e.currentTarget.setPointerCapture(e.pointerId)`. Pointer capture переводит `pointerup` на контейнер, поэтому итоговый `click` диспатчится в контейнер, а не в кнопку зоны — `onClick` кнопки (`Planet.tsx:259`, `setOpened(z.cat)`) не срабатывает.
Доказательство (Playwright, реальный клик мышью по кнопке «Зона „Саморазвитие“ — открыть»): интерьер не открылся; программный `el.click()` тут же открыл. На телефоне тап по зоне работать не будет — ключевой сценарий ТЗ «тап по зоне ныряет внутрь» сломан.
Рекомендация: не захватывать pointer на `pointerdown`, а ставить capture только после порога перемещения (например, |dx| больше 6px) в `onPointerMove`, либо не вызывать `setPointerCapture` вовсе.

**Д-2 (минор, зона scaffold/backend-supabase): нет `npm run lint`.**
ТЗ (раздел «Агент 3», п.1) требует прогон `lint`; в `package.json` скрипта нет, ESLint не в devDependencies, конфига нет. `npm run lint` даёт `npm error Missing script: "lint"`.

### Замечания (не дефекты ТЗ)

- Дата на Домой: «20 Июля» — месяц с заглавной; по нормам русской типографики «20 июля» (`Home.tsx`).
- Бандл 556.69 kB (больше 500 kB, предупреждение Vite) — стоит рассмотреть code-splitting.
- При идущем таймере кнопка «Продолжить» на Домой скрывается (заменяется пилюлей «Сейчас идёт») — выглядит осознанно, но в ТЗ не оговорено.
- Без ключей вместо логина показывается заглушка «заполните .env по SETUP.md» — ожидаемое поведение (10-login.png).

## Скриншоты (docs/screenshots/, 390x844 @2x)

| Файл | Что на нём |
|---|---|
| 01-home.png | Домой, без сессии |
| 02-timer.png | Таймер, без сессии |
| 03-stats.png | Статистика (День), без сессии |
| 04-planet.png | Планета, без сессии (герой «Отдыхает» в центре) |
| 05-categories.png | Категории, без сессии |
| 06-timer-running.png | Таймер идёт (00:01:05, «Идёт 2-я минута») |
| 07-home-running.png | Домой с пилюлей «Сейчас идёт» |
| 08-planet-running.png | Планета: герой работает в активной зоне, бабл тикает |
| 09-interior-mine.png | Интерьер «Шахта» при идущем таймере (открыт программно — см. Д-1) |
| 10-login.png | Без ?demo=1: заглушка «Supabase не настроен» — ожидаемо, ключей нет |
| 11-interior-corporation.png | Интерьер «Корпорация», спящий (без сессии) |
| 12-stop-celebration.png | Празднование после «Стоп» (+1 мин, задача «Матеша») |

## Деплой

**ЗАБЛОКИРОВАН:** wrangler не авторизован. Нужно `npx wrangler login`, затем `npm run deploy` (см. SETUP.md). После деплоя: smoke-прогон на публичном URL, ссылку — сюда и в README (ТЗ, «Публичная ссылка — Cloudflare»). README в корне пока нет — создать при добавлении ссылки.

## Что осталось владельцу

1. Вставить реальные ключи в `.env` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) по SETUP.md.
2. Выполнить `supabase/apply_all.sql` в SQL Editor Supabase.
3. Выключить Confirm email в Supabase Auth (иначе вход после регистрации потребует подтверждения письма).
4. `npx wrangler login`, затем `npm run deploy`; ссылку — Тимуру, в этот отчёт и README.
5. После пунктов 1-2 перепроверить заблокированные пункты: запись сессий в БД, мультиюзер-изоляцию, восстановление активной сессии после перезагрузки, сходимость статистики с `tt_category_totals()`/`tt_task_totals()`.
